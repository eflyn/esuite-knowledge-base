{
  "name": "@eflyn/esuite-knowledge-base",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preview": "rm -rdf preview; mkdir -p preview; node scripts/preview.mjs",
    "publish": "rm -rdf dist; mkdir -p dist; node scripts/publish.mjs && git add . && git commit -m \"published on `date`\"; npm run publish:upload",
    "publish:upload": "aws s3 sync ./dist s3://myeflyn/knowledge/`git symbolic-ref --short HEAD`  --endpoint=\"https://nyc3.digitaloceanspaces.com\"  --acl public-read"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^18.8.3",
    "marked": "^4.1.1",
    "rxjs": "^7.5.7"
  }
}
